#include "main.h"

/**
 * add - adds two a and b and returns the result
 * @n: int to be added to b
 * @m: int to be added to a
 * Return: sum of a and b
 */
int add(int n, int m)
{
	int k;

	k = n + m;

	return (k);
}
